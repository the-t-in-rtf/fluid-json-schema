{
    "type":        "object",
    "id":          "user.json",
    "title":       "Simple username/password validation schema.",
    "description": "This schema is used in testing our mock login form.",
    "definitions": {
        "username": {
            "type": "string",
            "description": "You must enter a username.",
            "minLength": 1
        },
        "password": {
            "type": "string",
            "description": "You must enter a password.",
            "minLength": 1
        }
    },
    "properties": {
        "username": {
            "$ref": "#/definitions/username"
        },
        "password": {
            "$ref": "#/definitions/password"
        }
    },
    "required": ["username", "password"]
}
