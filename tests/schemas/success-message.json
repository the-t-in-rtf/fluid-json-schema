{
    "type": "object",
    "id": "success-message.json",
    "description": "A copy of message.json, but for success.   Used to confirm header links are set appropriately.",
    "properties": {
        "isError": { "type": "boolean" },
        "message": { "type": "string" }
    },
    "required": ["message"]
}