// As this package provides multiple server-side components, we have separate includes for each of them.  To include
// just the validator, require this file.  To include the middleware as well, require the package itself.
//
require("./src/js/common/validate");
require("./src/js/server/validate");