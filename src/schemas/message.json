{
    "type": "object",
    "id": "message.json",
    "description": "The common format used for JSON error messages.",
    "properties": {
        "isError": { "$ref": "#/definitions/isError" },
        "message": { "$ref": "#/definitions/message" }
    },
    "definitions": {
        "isError": {
            "type": "boolean",
            "description": "Whether the request was completed successfully.  Set to 'true' if any problems occur."
        },
        "message": {
            "type": "string",
            "description": "An optional message summarising the results of a REST call."
        }
    },
    "required": ["message"]
}